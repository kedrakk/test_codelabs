import 'package:codelabs_one/codelabs_one_random_word.dart';
import 'package:flutter/material.dart';

class CodeLabsOnePage extends StatelessWidget {
  const CodeLabsOnePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: RandomWordPage(),
    );
  }
}
